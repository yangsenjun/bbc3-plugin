package ${packagePath};

import ${poPath}.${ModelName}PO;
import io.terminus.gaia.domain.BaseRepository;
import org.springframework.stereotype.Repository;

/**
* @author ${userName}
* @date ${date}
* description:
**/
@Repository
public class ${ModelName}Repository extends BaseRepository<${ModelName}PO> {
}