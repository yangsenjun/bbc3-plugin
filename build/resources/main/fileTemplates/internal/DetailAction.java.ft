package ${packagePath};

import ${voPath}.${ModelName}VO;
import io.terminus.trantorframework.api.annotation.Function;

/**
* @author ${userName}
* @date ${date}
* description:
**/

@Function
public interface ${ModelName}DetailAction {

    ${ModelName}VO execute(${ModelName}VO ${modelName}VO);

}
